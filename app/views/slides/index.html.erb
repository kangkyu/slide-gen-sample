<div class="bg-white">
  <div class="mb-8">
    <div id="flash_messages" class="mb-4"></div>

    <div class="flex justify-start mb-8">
      <%= link_to "New Slide", new_slide_path,
          class: "inline-flex items-center px-6 py-3 bg-emerald-600 text-white font-semibold rounded-lg hover:bg-emerald-700 focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:ring-offset-2 transition duration-200 shadow-sm" %>
    </div>

    <div id="slides">
      <%= render partial: "slide_item", collection: @slides, as: :slide %>
    </div>
  </div>
</div>
